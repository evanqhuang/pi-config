{
  "name": "pi-notes",
  "version": "0.1.0",
  "private": true,
  "description": "Session-local durable execution checkpoints for Pi",
  "type": "module",
  "keywords": ["pi-package", "pi-extension", "notes"],
  "pi": { "extensions": ["./index.ts"] },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.4",
    "@types/node": "^25.5.0",
    "typebox": "1.3.7",
    "typescript": "^6.0.0",
    "vitest": "^4.0.18"
  }
}
